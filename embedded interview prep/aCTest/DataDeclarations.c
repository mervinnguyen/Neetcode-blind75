//Using the variable a, write down definitions for the following

//a) an integer
int a = 0;

//b) pointer to an integer
int *p;

//c) pointer to a pointer to an integer
int a **p;

//d) an array of 10 integers
int a[10];

//e) an array of 10 pointers to integers
int *a[10];

//f) A pointer to an array of 10 integers
a[10] *p;       //correction: int (*a)(int);

//g) pointer to a function that takes an integer as an argument and returns and integer
int function(int) *p;       //correction: int (*a)(int);

//h) an array of 10 pointers to functions that take an integer argument and return an integer.
int function(int) *a[10];       //correction: int (*a[10])(int)

